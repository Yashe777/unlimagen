services:
  - type: web
    name: unlimagen
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 4 --threads 4 --worker-class gthread
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
