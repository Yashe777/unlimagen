<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Numidia AI</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            color: #ffffff;
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 1000px; margin: 0 auto; padding: 40px 20px; }
        h1 { font-size: 48px; margin-bottom: 40px; }
        .card {
            background: #2a2a2a;
            border: 2px solid #444444;
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
        }
        .card h2 { font-size: 24px; margin-bottom: 20px; color: #ffffff; }
        .plan-badge {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 10px;
            font-weight: 700;
            font-size: 18px;
            margin-bottom: 20px;
        }
        .plan-free { background: #444444; }
        .plan-basic { background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%); }
        .plan-pro { background: linear-gradient(135deg, #e0e7ff 0%, #f8fafc 100%); color: #1e293b; }
        .stat { display: flex; justify-content: space-between; padding: 15px 0; border-bottom: 1px solid #3a3a3a; }
        .stat:last-child { border-bottom: none; }
        .btn {
            display: inline-block;
            padding: 14px 30px;
            background: linear-gradient(135deg, #e0e7ff 0%, #f8fafc 100%);
            color: #1e293b;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 700;
            margin-top: 20px;
        }
        .back-link { margin-top: 30px; }
        .back-link a { color: #aaaaaa; text-decoration: none; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Dashboard</h1>
        
        <div class="card">
            <h2>Account Information</h2>
            <div class="stat">
                <span>Email:</span>
                <strong>{{ user.email }}</strong>
            </div>
            <div class="stat">
                <span>Current Plan:</span>
                <span class="plan-badge plan-{{ user.plan }}">{{ user.plan|upper }}</span>
            </div>
            <div class="stat">
                <span>Daily Limit:</span>
                <strong>{% if user.daily_limit == -1 %}Unlimited{% else %}{{ user.daily_limit }} images{% endif %}</strong>
            </div>
            <div class="stat">
                <span>Member Since:</span>
                <strong>{{ user.created[:10] }}</strong>
            </div>
        </div>
        
        <div class="card">
            <h2>Today's Usage</h2>
            <div class="stat">
                <span>Images Generated Today:</span>
                <strong>{{ usage }} / {% if user.daily_limit == -1 %}∞{% else %}{{ user.daily_limit }}{% endif %}</strong>
            </div>
            {% if user.plan == 'free' %}
            <a href="/pricing" class="btn">Upgrade Plan</a>
            {% endif %}
        </div>
        
        <div class="back-link">
            <a href="/">← Back to Generator</a>
        </div>
    </div>
</body>
</html>
